---
title: JobCopilot 当前与未完成任务
owner: lemma42796
last_updated: 2026-09-05
purpose: 只记录当前任务和尚未完成、仍计划执行的任务。
---

# 维护规则

- `STATUS.md` 记录已经发生的当前事实;本文只记录还要做的事。
- 每项任务写目标、依赖和状态;改动点与验收标准写在 `CONCURRENCY_PLAN.md` 对应阶段,本文不复制。
- 完成后从本文移除;完成历史由 Git commit / tag 承担。

# 已确定的商业形态

- 多用户线上服务,共用平台 API key,用户不接触 key。
- 用户预充值余额,按官方 API 价格实扣,余额耗尽即禁用。
- 充值链路本阶段只做模拟,不实现真实支付。

# 当前任务

## 并发改造的运行验证

状态:**待执行**

P0–P8 代码已全部落地,但一次都没跑起来过。按下列顺序验证,每步失败即停:

1. `uv sync --all-packages`(新增 redis / gunicorn 依赖)
2. `alembic upgrade head`,确认 0025 / 0026 单头、且在**有存量数据的库**上把旧数据正确归到 legacy 用户
3. `docker compose up -d`,确认 8 个服务健康,worker 副本能领任务
4. 注册两个用户,交叉访问对方的 note / session / jd,确认一律 404 —— 这是 P0 的实质验收
5. 跑一场完整面试,确认 202 + job_id、SSE 断线用 `after_seq` 续看、`llm_calls` 三条链路都有记录且余额按 `cost_cny` 递减
6. 强杀一个 worker,确认未 ack 的 job 被另一副本接管且不重复扣费

`user_id` 过滤位置清单见本次交付报告,需逐条核对。

## 压测基线

状态:**未开始**

目标:执行 `loadtest/` 下的 k6 脚本,用实测值替换 `CONCURRENCY_PLAN.md` 中全部推算数字(单次调用真实 token 分布、单场面试真实成本、队列水位阈值)。

依赖:上述运行验证通过。

# 未完成任务

## 失败 job 的自动重试

目标:当前 worker 崩在 `running` 中途的 job 只被回收器打终态,不自动重试。需要按 kind 区分可安全重放的任务,补重试与重试上限。

依赖:压测基线,确认重试不会放大成本。

## 语义缓存阈值评测

目标:语义缓存默认关闭。需要评测近似命中阈值下的误命中率,确认不会把不同问题的答案串台,再决定是否默认开启。

## 真实支付链路

目标:替换模拟充值。当前不启动。

# 已推迟

## 全局并发闸门改为 Redis 实现

实测上游允许约 333 个并发调用,4 进程 × 32 闸门合计 128 仍在余量内,该阶段暂不产生收益。触发条件与届时改动点见 `CONCURRENCY_PLAN.md` 附录。

# 不在本文档范围

- 项目最新状态 → `STATUS.md`
- 稳定技术架构与约束 → `TECH_DESIGN.md`
- 并发改造的改动点与验收标准 → `CONCURRENCY_PLAN.md`
- 评测方法与证据 → `../evals/EVAL_GUIDE.md`
