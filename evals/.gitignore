# Raw screenshots — never commit (Boss/拉勾/招聘邮件原件,版权敏感)
raw/

# Promptfoo cache + reports
.promptfoo/
reports/
cache/

# Local prep workspace (review files / per-entry edits / dry-run logs)
tmp/

# Node
node_modules/
*.log

# Local overrides
.env
.env.local
